# 量化研究平台 · SOP v2(生效:2026-08-24 人类放行)

> 正本:`D:\qmt_strategy\改造方案\量化研究平台_SOP_v2.md`;本文件为仓库内施工用副本,修订须两处同步并记账。

> 适用范围:quant_platform(记录想法/数据清洗/研发/回测/信号)。**执行与运维出界**,未来另立执行SOP。
> 相对 v1.1 的变化:①预注册定期复核条款随 f1f8_v2 废弃移除,崩溃定义制直接生效;②新增多源数据条款与运行纪律;③交易流水账条款移出(执行出界),改为信号交付契约;④红队AI干净上下文成文。
> 修订记录 2026-08-24:审稿并入——新增"阈值治理"总则,一切数值判据 config 化;active 因子不设上限;红线清单按品种包挂载,条文不再列举品种条目。

## 阈值治理(总则)
本SOP条文只锁**机制**;一切数值判据(IC/ICIR门槛、复现容差、冗余相关阈值、交叉验证容差、新鲜度红线等)都是 `config.protocol` 中的**可改默认值**,不写死在条文或代码里。修改自由,但两条机制不可配置:①**批前声明**——本批检验/回测开始前书面声明本批所用阈值(判据先于数据);②**修订记账**——改动追加记录,原值不删。

## S0 想法登记
自有想法记 `research/ideas.md`;外部输入(论文/研报/博客/帖子,不论载体)按 `research/papers/` 登记规则入库(规则自 cb_quant 平移)。每条:一行可证伪假设 + 数据可得性预判 + 目标品种。写不出证伪条件的不进S1。

## S1 数据审计
四项标配:退市样本在否 / 时点正确性(PIT)/ 脏值扫描 / 逐日覆盖曲线。
新增两条例行:**多源一致性抽检**(主源vs备源随机抽样对账,超容差报警)、DATA_DICTIONARY 字段单位表同步。
放行:核心字段无结构性缺失,缺陷有书面声明。

## S2 因子构建
- **防挖矿协议**(自 cb_quant 平移,资产无关):**样本内外切分与本批阈值在检验开始前书面声明**(判据先于数据);样本外每因子只评估一次,结果即终审;BH多重比较校正;`rejected`/`tested_weak` 永不删除;**active 数量不设上限**;对照组强制(阳性=已知因子复现达 config 容差,阴性=随机因子必判无效)。
- **经济学原理必填**:五要素注册(id/公式/方向/hypothesis/category),讲不出为什么赚钱的不收。
- 工具:alphalens tear sheet 一律经平台薄封装调用(T+1 价格对齐锁死在封装入口),**裸调用视为违规**。
- 放行:active,或 tested_weak 且与 active 冗余相关低于 config 阈值。

## S3 策略研发
- 原型先归类:截面选券 / 时序配置(首期两类;套利/事件/增强未来扩展)。
- 可解释性优先:线性打分为主;复杂模型必须搭配简单基线持续对照、偏离预警。
- **参数平原**:敏感性扫描呈现平原/悬崖,悬崖参数不用;禁止样本内单点调优。
- 组合搜索适用防挖矿协议:样本外只看一次 / 尝试次数登记 / 判据先写死 / **终选为空合法**。

## S4 回测与验证
- **真实性红线逐条核对**:按该策略所属**品种包挂载的红线清单**执行(机制见蓝图第七节)——清单属于品种包,不属于平台;新品种挂新清单,本条文不列举任何品种的具体条目。
- 基准:策略配置声明(≥1条,鼓励2条),超额只对配置基准计算;平台不预设基准。
- **AI对抗式校验**:红队AI必须**干净上下文**(只喂报告与说明书,禁止使用参与建设的会话);产出结构化质疑清单+逐条回应记录;回应不了的降级为局限声明或打回S3。
- 交叉验证(任一):独立引擎复现(偏差容差见 config)/ 异源数据对账 / 外部研报对标。
- 局限声明:报告尾部强制。

## S5 信号交付(本平台终点)
- 放行三件套:策略说明书 + **崩溃定义** + config独立小节;人类签字。
- **崩溃定义制**:策略无预定复核日,默认永续。放行前写死"实质性崩溃"条件(示例:①回撤>历史最大×1.5 ②滚动两年跑输基准且执行记录证明无误 ③依赖的市场机制结构性改变),触发任一→强制一次性人工复核,人工终审;退役策略研究记录永久保留。
- **信号文件契约**:`orders_{date}.csv` 固定schema(策略id/日期/代码/方向/目标权重/信号参考价/数据新鲜度),供未来执行服务直接消费;**数据落后超过 config 新鲜度红线的禁止出信号**(按策略/品种配置,example 给默认值)。
- 自动/半自动执行是下游执行服务的选择,本平台不感知。

## 横向制度
- **A 任务卡**:规格明确的施工一卡一会话(定义/公式/判据/验收四段);探索型豁免立卡,结论当日落CHANGELOG。
- **B 变更三级**:措辞→直改;参数判据→修订记账(原文不动追加);协议级(本SOP/防挖矿协议/红线清单)→书面提案+人类裁决。
- **C 文档体系**:CHANGELOG(时间线)/ 看板(在途)/ 策略说明书(每策略宪法)/ ideas与papers登记 / CLAUDE.md(指针)。**对话不是记录**:AI对话中的重要判断当日落盘,否则视为未发生。
- **D 数据治理**:凭证本地+pre-commit扫描;代码与文档每次卡收尾 push GitHub 私有仓(异地备份);cache 不备份(可由fetcher重建);多源对账超容差报警。
- **E 运行纪律(新)**:每周8小时建议分配 = 1小时运营(数据更新/巡检/验收)+ 7小时研究;数据新鲜度红线见S5。
- **F 三顶帽子**:运营官(执行,禁研究冲动)/ 研究员(S0~S4,受协议约束)/ 风控官(裁决,只认判据)——一人分饰,同一时刻只戴一顶。AI分工:对话AI=方案与红队,Claude Code=施工与验收,人类=终审。

## 十条军规 v2
1 判据先于数据  2 样本外只看一次  3 对照组必带  4 终选为空合法  5 全成本+真实性红线
6 来源不写死  7 变更必记账  8 对话不是记录  9 数据不新鲜不出信号  10 帽子分开戴
