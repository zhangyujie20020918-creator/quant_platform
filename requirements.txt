pandas==2.2.3
PyYAML==6.0.2
pytest==8.3.4
numpy<2                 # rqalpha 6.3.0 在 py3.11 下要求 numpy<2
rqalpha==6.3.0          # 卡2 裁决:生产回测引擎(数据源换成我们的 store,不用 bundle)
pyarrow>=17             # parquet 读写与下推过滤
alphalens-reloaded==0.4.6   # 卡4:因子 tear sheet(只经 factors/alphalens_wrapper 调用,T+1 口径锁死)
quantstats==0.0.81          # 卡4:净值绩效 html 报告(backtest/quantstats_report)
